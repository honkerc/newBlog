{
    "name": "mini",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "serve": "vue-cli-service serve",
        "build": "vue-cli-service build",
        "sync": "vue-cli-service build && npx cap sync",
        "android:open": "npx cap open android",
        "android:build": "vue-cli-service build && npx cap sync && npx cap copy android && cd android && gradlew assembleDebug"
    },
    "dependencies": {
        "vue": "^3.2.13",
        "vue-router": "^4.0.3",
        "@capacitor/core": "^7.0.0",
        "@capacitor/android": "^7.0.0"
    },
    "devDependencies": {
        "@vue/cli-service": "~5.0.0",
        "@capacitor/cli": "^7.0.0"
    },
    "browserslist": [
        "> 1%",
        "last 2 versions",
        "not dead",
        "not ie 11"
    ]
}